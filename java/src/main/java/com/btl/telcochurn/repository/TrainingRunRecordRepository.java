package com.btl.telcochurn.repository;

import com.btl.telcochurn.entity.TrainingRunRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrainingRunRecordRepository extends JpaRepository<TrainingRunRecord, Long> {
}
